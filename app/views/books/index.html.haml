%p#notice= notice
%h1 Books
- @book_sum.each do |key, value|
  %h3
    = key
    = value
%table
  %thead
    %tr
      %th Isbn
      %th Title
      %th Price
      %th Publish
      %th Published
      %th Dl
      %th{:colspan => "3"}
  %tbody
    - @books.each do |book|
      %tr
        %td= book.isbn
        %td= book.title
        %td= book.price
        %td= book.publish
        %td= book.published
        %td= book.dl
        %td= link_to 'Show', book
        %td= link_to 'Edit', edit_book_path(book)
        %td= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Book', new_book_path
